/**
 * Copy owned by this deployment's brand.
 *
 * A product name is a proper noun, so this dictionary holds the deployment's
 * identity rather than a per-language table; the file name is what makes it the
 * sanctioned owner of the string presentation code may not carry itself.
 */

/**
 * Deployment name rendered beside the sidebar mark. The document title is a
 * separate build-time value (`DSH_CLIENT_TITLE`); both must name the same
 * product, and nothing here can enforce that.
 */
export const PRODUCT_NAME = 'Highcom Work'
